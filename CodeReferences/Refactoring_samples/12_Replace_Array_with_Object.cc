#include "12_Replace_Array_with_Object.h"
#include <stdlib.h>


#include <string>
#include <vector>
#include <tuple>

//char row[3][50];

//int setScore(char* name, char* wins, char* losses)
//{
//	strcpy(row[0], name);
//	strcpy(row[1], wins);
//	strcpy(row[2], losses);
//	return atoi(row[1]) + atoi(row[2]);
//}

void Score::SetScore(string name, int wins, int losses)
{
//	score.
}
