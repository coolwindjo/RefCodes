1. 아래와 같은 linear regression 모델을 구성하세요
   ㄱ.  x_data => [2000, 3] 랜덤 값을 구성하여 사용한다.
   ㄴ.  w = [0.3, 0.5, 0.1],  b = -0.2를 사용한다.
   ㄷ.  그래프 객체를 새로 구성한다.
   ㄹ.  y = wx + b의 모델을 사용한다.
   ㅁ.  w,b, y node는 'inference' 네임스코프 영역에 할당한다.
   ㅂ.  loss node 'loss layer' 네임스코프 영역에 할당한다.
   ㅅ. 'train'네임스코프 영역에는 학습에 필요한 node들을 구성한다.
   ㅇ. 100번을 학습시키고, 5번의 학습 당 w,b 값을 출력한다.
 