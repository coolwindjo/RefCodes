{
     "name": "flutter_docker",
     "context": "..",
     "dockerFile": "../Dockerfile",
    //  "remoteUser": "coolwind",
    //  "mounts": [
    //      "source=/dev/bus/usb,target=/dev/bus/usb,type=bind"
    //  ],
     "settings": {
         "terminal.integrated.shell.linux": null
     },
     "runArgs": ["--privileged"],
     "extensions": ["dart-code.flutter"],
     "workspaceMount": "source=${localWorkspaceFolder}/workspace,target=/home/workspace,type=bind,consistency=delegated",
    //  "workspaceMount": "source=${localWorkspaceFolder}/workspace,target=/home/coolwind/workspace,type=bind,consistency=delegated",
     "workspaceFolder": "/home/workspace"
}
